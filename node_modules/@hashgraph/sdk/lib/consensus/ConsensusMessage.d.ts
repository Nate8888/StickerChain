import { ConsensusTopicResponse } from "../generated/MirrorConsensusService_pb";
import { ConsensusTopicId } from "./ConsensusTopicId";
import { Time } from "../Time";
export declare class ConsensusMessage {
    readonly topicId: ConsensusTopicId;
    readonly consensusTimestamp: Time;
    readonly message: Uint8Array;
    readonly runningHash: Uint8Array;
    readonly sequenceNumber: number;
    constructor(topicId: ConsensusTopicId, resp: ConsensusTopicResponse);
    toString(): string;
}
