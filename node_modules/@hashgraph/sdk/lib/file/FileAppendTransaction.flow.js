/**
 * Flowtype definitions for FileAppendTransaction
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.10.0
 */

import {
          TransactionBuilder
        } from '../TransactionBuilder';
import {
          Transaction
        } from '../generated/Transaction_pb';
import {
          TransactionResponse
        } from '../generated/TransactionResponse_pb';
import {
          grpc
        } from '@improbable-eng/grpc-web';
import {
          FileIdLike
        } from '../file/FileId';

/**
 * Append the given contents to the end of the file. If a file is too big to create with a single
 * `FileCreateTransaction``, then it can be created with the first part of its contents, and then
 * appended multiple times to create the entire file.
 */
declare export class FileAppendTransaction mixins TransactionBuilder {
constructor(): this;

/**
 * The file ID of the file to which the bytes are appended to.
 */
setFileId(fileId: FileIdLike): this;

/**
 * The bytes to append to the contents of the file.
 */
setContents(contents: Uint8Array | string): this;
_doValidate(errors: string[]): void;
/* NO PRINT IMPLEMENTED: GetAccessor */ any;
}